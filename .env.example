POSTGRES_DB=annecybike
POSTGRES_USER=annecybike
POSTGRES_PASSWORD=changeme_strong_password

# Port HTTP exposé (défaut: 80)
HTTP_PORT=80

# Intervalle de polling GBFS en secondes (défaut: 60)
POLL_INTERVAL_SECONDS=60
